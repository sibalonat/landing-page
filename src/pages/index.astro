---
import Layout from "../layouts/Layout.astro";
import redG from "../assets/gka/red-g.svg";
import mtksLogo from "../assets/mtks/mtks-logo.svg";
---

<Layout
  title="Galeria Kombëtare e Arteve | National Gallery of Arts"
  description="Faqja zyrtare e Galerisë Kombëtare të Arteve në Shqipëri."
  email="info@galeriakombetare.gov.al"
>
  <!-- Logos slot content -->
  <div slot="logos" class="absolute inset-4 sm:inset-8 md:inset-12">
    <!-- GKA Logo Container -->
    <div
      id="gka-logo-container"
      class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-full max-w-[280px] sm:max-w-md md:w-125 lg:w-150"
    >
      <div class="relative flex items-center">
        <!-- Static red G - always visible -->
        <img
          src={redG.src}
          alt=""
          class="w-20 sm:w-28 md:w-35 lg:w-57.5 h-auto object-contain pointer-events-none shrink-0"
        />

        <!-- Text Container - positioned to the right of the G -->
        <div class="ml-0 flex-1 flex items-center font-mulish font-medium">
          <!-- English text - fades out -->
          <div
            id="text-en"
            class="-ml-20 sm:-ml-28 md:-ml-35 -mt-1 sm:-mt-2 md:-mt-3 text-black text-sm sm:text-xl md:text-2xl lg:text-[1.8rem]"
            style="letter-spacing: 0.05em; line-height: 1;"
          >
            <div class="capitalize">National</div>
            <div class="capitalize">Gallery</div>
            <div>of <span class="capitalize">Arts</span></div>
          </div>

          <!-- Albanian text - fades in -->
          <div
            id="text-al"
            class="-ml-17 sm:-ml-24 md:-ml-30 -mt-1 sm:-mt-2 md:-mt-3 text-black opacity-0 text-sm sm:text-xl md:text-2xl lg:text-[1.8rem]"
            style="letter-spacing: 0.05em; line-height: 1;"
          >
            <div class="capitalize">Galeria</div>
            <div class="capitalize">Kombëtare</div>
            <div>e <span class="capitalize">Arteve</span></div>
          </div>
        </div>
      </div>
    </div>

    <!-- MTKS logo -->
    <img
      id="mtks-logo"
      src={mtksLogo.src}
      alt="MTKS logo"
      class="absolute top-4 sm:top-8 md:top-15 right-2 sm:right-4 w-20 sm:w-28 md:w-40 lg:w-66 h-auto opacity-0"
    />
  </div>
</Layout>

<script>
  import { gsap } from "gsap";

  document.addEventListener("DOMContentLoaded", () => {
    const isMobile = window.innerWidth < 640;
    const isTablet = window.innerWidth >= 640 && window.innerWidth < 1024;

    const tl = gsap.timeline();

    // 1. Fade out English text
    tl.to("#text-en", {
      opacity: 0,
      duration: isMobile ? 0.4 : 0.6,
      delay: isMobile ? 1 : 1.5,
    });

    // 2. Fade in Albanian text
    tl.to(
      "#text-al",
      {
        opacity: 1,
        duration: isMobile ? 0.4 : 0.6,
      },
      "+=0.3"
    );

    // 3. Move the entire GKA logo container to top-left with responsive positioning
    tl.to(
      "#gka-logo-container",
      {
        left: isMobile ? "0.5rem" : isTablet ? "0.75rem" : "1rem",
        top: isMobile ? "0.5rem" : isTablet ? "1rem" : "2rem",
        x: 0,
        y: 0,
        scale: isMobile ? 0.65 : isTablet ? 0.7 : 0.8,
        duration: isMobile ? 0.8 : 1,
        ease: "power2.inOut",
      },
      "+=0.5"
    );

    // 4. Fade in MTKS logo
    tl.to(
      "#mtks-logo",
      {
        opacity: 1,
        duration: isMobile ? 0.6 : 0.8,
      },
      "-=0.5"
    );
  });
</script>
